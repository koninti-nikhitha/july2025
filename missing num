class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        n=len(nums)
        xor_all=0
        xor_nums=0
        for i in range(1,n+1):
            xor_all^=i
        for i in nums:
            xor_nums^=i
        return xor_all^xor_nums

        
